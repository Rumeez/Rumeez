import React from 'react';

export function User_Home_Page(): JSX.Element {
  return(
    <div> 
      <p> Logged in :p! User Home Page</p>
    </div>
  )
}

/*
<Link to="/userInfo">
    <button onClick={onClick}>
      {children}
    </button>
  </Link>
*/ 